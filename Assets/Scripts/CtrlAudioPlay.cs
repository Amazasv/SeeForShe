using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[RequireComponent(typeof(AudioSource))]
public class CtrlAudioPlay : MonoBehaviour
{
    //[SerializeField]
    //private int StartScene = 0;
    //[SerializeField]
    //private int EndScene = 0;

    //private AudioSource a = null;
    //private void Awake()
    //{
    //    a = GetComponent<AudioSource>();
    //}
    //private void Update()
    //{
    //    //if (a.enabled && !(GameManager.sceneIndex >= StartScene && GameManager.sceneIndex <= EndScene)) a.enabled = false;
    //    //if (!a.enabled && GameManager.sceneIndex >= StartScene && GameManager.sceneIndex <= EndScene) a.enabled = true;
    //}
}
